<Window x:Class="SnakeGame.Client.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="150" Width="350"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Введите ваше имя:" FontWeight="Bold"/>
        <TextBox x:Name="NameTextBox" Grid.Row="1" Margin="0,8,0,0"/>
        <Button Content="Играть" Grid.Row="2" Margin="0,12,0,0" Width="80"
                HorizontalAlignment="Right" Click="Play_Click"/>
    </Grid>
</Window>